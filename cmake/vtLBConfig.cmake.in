include(CMakeFindDependencyMacro)

find_dependency(comm REQUIRED CONFIG)

if(@vt_backend_feature_enabled@)
  find_dependency(vt REQUIRED CONFIG)
else()
  find_dependency(magistrate REQUIRED CONFIG)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/vtLBTargets.cmake")
